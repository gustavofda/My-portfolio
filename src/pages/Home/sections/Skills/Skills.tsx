

interface SkillsProps {
  id: string;
}

const Skills: React.FC<SkillsProps> = ({ id }) => {
  return (
    <div id={id}>
      {/* Conteúdo do componente Skills */}
    </div>
  );
}

export default Skills;